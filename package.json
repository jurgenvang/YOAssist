{
  "name": "yoassist",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev",
    "test": "node test/sync.test.mjs && node test/zicht.test.mjs",
    "db:local": "wrangler d1 execute yoassist --local --file=./schema.sql",
    "db:remote": "wrangler d1 execute yoassist --remote --file=./schema.sql",
    "cron:deploy": "cd cron && wrangler deploy"
  },
  "devDependencies": {
    "better-sqlite3": "^11.10.0",
    "wrangler": "^3.0.0"
  }
}
